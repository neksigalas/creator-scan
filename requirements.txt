google-api-python-client==2.108.0
requests>=2.34.2
python-dotenv==1.0.0
rich==13.7.0
tqdm==4.66.1
fastapi==0.115.0
uvicorn==0.30.6
supabase==2.10.0
httpx==0.27.2
cryptography>=42
# Playwright is for local scanning only and must NOT be here: Vercel installs
# this file, and on 14/9/2026 an unpinned playwright pulled greenlet 3.5.6,
# which has no Linux wheel, so every deploy failed. See requirements-local.txt.
